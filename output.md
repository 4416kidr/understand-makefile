# MEMO

```bash
$ make cdir-split
cd sub
pwd
[work-dir]/understand-makefile
echo "different with shell script shebang"
different with shell script shebang
```

```bash
$ make cdir-oneline
cd sub && pwd
[work-dir]/understand-makefile/sub
echo "if you want to change directory, you need write in oneline"
if you want to change directory, you need write in oneline
pwd
[work-dir]/understand-makefile
```
